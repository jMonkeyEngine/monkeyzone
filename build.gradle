apply plugin: 'base'

buildscript {
    repositories {
        jcenter()
    }
    dependencies {

    }
}

subprojects {
    ext {
        jme = [group: 'org.jmonkeyengine', version: '3.1.0-beta1']
    }
    apply from: rootProject.file('common.gradle')
}